import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  typography: {
    button: {
      fontWeight: 500,
    },
  },
  palette: {
    primary: {
      main: '#EF5742',
    },
  },
})

export default theme
